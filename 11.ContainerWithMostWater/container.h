#include <vector>

using namespace std;

namespace baseline{
    class Solution {
    public:
        int maxArea(vector<int>& height);
    };
}

namespace BestSolution{
    class Solution {
    public:
        int maxArea(vector<int>& height);
    };
}