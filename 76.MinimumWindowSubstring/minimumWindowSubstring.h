#include <string>

using namespace std;

namespace baseline{
    class Solution {
    public:
        string minWindow(string s, string t);
    };
}

namespace twoPointers{
    class Solution {
    public:
        string minWindow(string s, string t);
    };
}