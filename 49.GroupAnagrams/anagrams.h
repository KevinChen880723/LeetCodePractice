#include <vector>
#include <string>

using namespace std;

namespace bruteforce{
    class Solution {
    public:
        vector<vector<string> > groupAnagrams(vector<string>& strs);
    };
}

namespace hashTable{
    class Solution {
    public:
        vector<vector<string> > groupAnagrams(vector<string>& strs);
    };
}