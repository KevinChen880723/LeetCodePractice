#include <algorithm>
#include "twosum.h"



vector<int> twosum::twoSum(vector<int>& nums, int target){
    int len = nums.size();
    sort(nums.begin(), nums.end());
    
    return vector<int>(10);
}