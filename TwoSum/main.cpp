#include "twosum.h"
#include <iostream>
#include <vector>

using namespace std;
int main(){
    // twosum obj;
    // vector<int> numList(10);
    // obj.twoSum(numList, 10);
    cout << "hello" << endl;
    return 0;
}